require('dotenv').config()
const path = require('path')

module.exports = {
    config: path.resolve('src', 'config', 'config.ts'),
    migrationsPath: path.resolve('dist', 'migrations'),
    seedersPath: path.resolve('dist', 'seeders'),
}
